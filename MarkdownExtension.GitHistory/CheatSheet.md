# Git history table
<code>
```git-history
show = "tags" #commits
fields = "date{D}|version|message|author{name}"
```
</code>

```git-history
show = "tags" #commits
fields = "date{D}|version|message|author{name}"
```